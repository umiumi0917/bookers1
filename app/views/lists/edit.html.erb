<h1>Editing Book</h1>
<%= form_with model: @book, url: list_path(@book.id), method: :patch do |f| %>
<h4>Title</h4>
  <%= f.text_field :title %>
<h4>Body</h4>
  <%= f.text_area :body %>
<%= f.submit 'Update Book' %>
<a href = "/lists/<%= @book.id %>">Show</a>
<a href="/lists">Back</a>
<% end %>